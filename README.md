# Qwen Auth Plugin

Adds OAuth auth support for Qwen Code in Nodecode.

## Install

```bash
nodecode plugin install https://github.com/nodecode-ai/nodecode-qwen-code-auth-plugin
```

## Use

```bash
nodecode auth login qwen
```

This plugin uses OAuth device flow with PKCE for Qwen (`chat.qwen.ai`).
